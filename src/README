jacServer

src      - source file
makefile - build with Makefile
cmake    - build with CMake

useing muduo as the base net framework

find the install path of muduo lib
/home/admin/frankz/github/build/release-install

1) build with CMake
mkdir build
cd build
cmake ../cmake
make
# echo binary is in ./bin/

2) build with GNU make
cd makefile
make
# echo binary is in ./
